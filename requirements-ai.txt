# Extras de IA / voz / visão do BMO — OPCIONAIS (o app roda sem eles, com
# os recursos mostrando "indisponivel"). Instale no Pi quando quiser ativar:
#     pip install -r requirements-ai.txt
#
# Dependências de sistema (Raspberry Pi OS):
#     sudo apt install libportaudio2 python3-opencv
#
# Wake word "BMO": crie a keyword no Picovoice Console, baixe o .ppn pra
# bmo_os/assets/bmo.ppn e ponha PORCUPINE_ACCESS_KEY no .env.
# Whisper: pywhispercpp baixa o modelo ggml na 1a vez (ou aponte WHISPER_MODEL).

sounddevice>=0.4.6        # captura do microfone (audição)
pywhispercpp>=1.2.0       # Whisper.cpp — transcrição (modelo small)
pvporcupine>=3.0.0        # wake word "BMO"
# opencv-python>=4.5.0    # webcam USB no PC (no Pi prefira: apt install python3-opencv)
