# Extras de IA / voz / visão do BMO — OPCIONAIS (o app roda sem eles, com
# os recursos mostrando "indisponivel"). Instale no Pi quando quiser ativar:
#     pip install -r requirements-ai.txt
#
# Dependências de sistema (Raspberry Pi OS):
#     sudo apt install libportaudio2 python3-opencv
#
# Wake word "BIMO": crie a keyword no Picovoice Console, baixe o .ppn pra
# bmo_os/assets/bimo.ppn e ponha PORCUPINE_ACCESS_KEY no .env.
# Transcrição: RECOMENDADO usar a API (Groq) no .env (STT_API_KEY) — tira o
# calor da Pi. O pywhispercpp (local) só é necessário com STT_BACKEND=local.

sounddevice>=0.4.6        # captura do microfone (audição) — sempre necessário
pvporcupine>=3.0.0        # wake word "BIMO"
# pywhispercpp>=1.2.0     # Whisper.cpp LOCAL (esquenta o Pi) — só p/ STT_BACKEND=local
# opencv-python>=4.5.0    # webcam USB no PC (no Pi prefira: apt install python3-opencv)
